/* =====================================================================
   Vacation Math — AI Config
   Set your Perplexity API key here to enable the AI Live Pricing layer.

   SECURITY NOTE: This key is visible in the browser source. For a
   production deployment, proxy requests through a Cloudflare Worker
   or serverless function instead of exposing the key client-side.
   Rate limiting (10 queries/session) is enforced in ai-live-pricing.js.
   ===================================================================== */
var VM_AI_CONFIG = {
  perplexityKey: "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"
};
